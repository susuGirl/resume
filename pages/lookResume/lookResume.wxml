<view>
    <view wx:if="{{ shareResumeId }}">
        <!-- 用canvas画五角星 -->
        <icon type="success" size="20" color="green"></icon>
        <text>collect -- {{ shareResumeId }}</text>
    </view>
    <view wx:else>
        <!-- 用样式画开关 -->
        <view>public</view> 
        <view>private</view>
    </view>
    <homepagebtn
        catch:baseInfoTap="baseInfoTap"
        catch:workInfoTap="workInfoTap"
        catch:otherInfoTap="otherInfoTap"
        catch:selfIntroductionTap="selfIntroductionTap"
    >

    </homepagebtn>

</view>
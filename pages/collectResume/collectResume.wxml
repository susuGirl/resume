<view>
    <view class="headerStyle">collect resume <text class="textStyle">{{ collectResume.length }}</text>/piece</view>

    <lookresume
         resumeInfo="{{ collectResume }}"
         bind:handleDelete="handleCancelCollection"
    >
    </lookresume>
</view>